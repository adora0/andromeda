-include ./.env.local
include ./envfile

CFLAGS=-O2 -g
CPPFLAGS=
LDFLAGS=
ASFLAGS=--warn

COMPONENTS=\
boot \
libc \
kernel

PROJECT_NAME=cosmos

BUILDDIR:=$(shell realpath $(BUILDDIR))
SRCDIR:=$(PWD)/src
TOOLSDIR:=$(PWD)/tools

SYSROOT=$(BUILDDIR)/sysroot
INSTALL_PREFIX=/usr
INSTALL_EXEC_PREFIX=$(INSTALL_PREFIX)
INSTALL_INCLUDEDIR=$(INSTALL_PREFIX)/include
INSTALL_LIBDIR=$(INSTALL_PREFIX)/lib
INSTALL_BOOTDIR=$(INSTALL_PREFIX)/boot